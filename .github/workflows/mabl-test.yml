name: Run Mabl Tests

on:
  push:
    branches: [ main ]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Mabl Tests
        uses: mablhq/github-run-tests-action@v1
        with:
          mabl-api-key: ${{ IgiPxUfpMNVaco9Qlvbgaw}}
          application-id: PGsSWMBxP0Zyu8cqeNqaIw
          environment-id: G44nkDyesCuhPyzrPHg7UQ
       

